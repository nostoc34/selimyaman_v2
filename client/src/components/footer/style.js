import { createUseStyles } from "react-jss";

const footerStyles = createUseStyles({
	footer: {
		textAlign: "center",
		paddingBottom: "10px",
		paddingTop: "30px"
	},
});

export default footerStyles;
